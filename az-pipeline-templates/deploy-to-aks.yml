# File: az-pipeline-templates/deploy-to-aks.yml
steps:
  displayName: 'Helm Install'
- script: helm --debug=true install --name=${{ parameters.acr-name }} --values=${{ parameters.values-dir }}/${{ parameters.values-file }} ${{ parameters.values-dir }}